# Used only by train_yolo_detector.py for an optional custom full-scene model.
path: ../data/parking_yolo
train: images/train
val: images/validation
test: images/test

names:
  0: parking_space
  1: vehicle
